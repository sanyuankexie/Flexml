package com.facebook.litho

open class ComponentTree2(builder: Builder?) : ComponentTree(builder) {
    override fun attach() {
        super.attach()
    }

    override fun detach() {
        super.detach()
    }
}